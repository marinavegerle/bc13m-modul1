package lesson13;

public class Triangle {
    public static void main(String[] args) {
        //firstTriangle(10);
        second(10);
    }

    public static void firstTriangle(int line) {
        String divider = " ";
        String output2 = "";
        String star = "*";
        String lineDivider = "";
        for (int i = 0; i < line; i++){

        }
        String empty = "";
        for(int i = 0; i<line;i++){
            empty = lineDivider.substring(0, line);
        }
        //String finalVariable =
    }
public static void second(int line){
    String star = "*";
    String space = "         ";
    for (int i = 0; i < line; i++);
    space = space + star;
    space = space.substring(1, space.length()-1);
    System.out.println(space += star);
}

}
